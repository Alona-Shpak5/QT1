#ifndef SENSORMETRIC_HPP
#define SENSORMETRIC_HPP

#include <QString>


struct SensorMetric {
    QString name;   // Назва сенсора
    int value;      // Значення, яке генерує сенсор
};

#endif // SENSORMETRIC_HPP
